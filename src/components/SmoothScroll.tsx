"use client";

import { useEffect } from "react";

export default function SmoothScroll({ children }: { children: React.ReactNode }) {
  useEffect(() => {
    // Placeholder for smooth scroll functionality
  }, []);

  return <>{children}</>;
}
